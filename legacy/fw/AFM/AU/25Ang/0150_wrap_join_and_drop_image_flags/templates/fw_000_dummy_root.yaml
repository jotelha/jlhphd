{% extends "fw_base.yaml" %}
{% block body %}
name: "{{ machine }}, {{ title }}, dummy root"
spec:
  _category: {{ worker }}
  _tasks:
  - _fw_name: ScriptTask
    script: 'echo "Dummy root FireWorks"'
    use_shell: true
    fizzle_bad_rc: false

  metadata:
    state: "dummy root"
    {{ render_metadata()|indent(4)}}
{% endblock %}
