{% extends "fw_base.yaml" %}
{% block body %}
name: "{{ machine|default("NEMO",true) }}, {{ title }}, AFM force-distance curve production input files retrieval"
spec:
  _category: {{ worker|default("nemo_noqueue",true) }}
  _files_out:
    input_header:               lmp_header.input
    input_production:           lmp_production.input
    coeff_file:                 coeff_hybrid.input

  _tasks:

  - _fw_name: GetFilesTask
    identifiers:
    - lmp_header.input
    - lmp_production.input

  - _fw_name: GetFilesTask
    identifiers:
    - {{ coeff_identifier }}
    new_file_names:
    - coeff_hybrid.input

  metadata:
    state: production input files
    {{ render_metadata()|indent(4)}}
{% endblock %}
