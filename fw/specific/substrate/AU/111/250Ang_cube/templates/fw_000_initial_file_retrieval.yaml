{% extends "fw_base.yaml" %}
{% block body %}
name: "{{ machine|default("NEMO",true) }}, {{ title }}, EAM substrate only, file retrieval and preprocessing"
spec:
  _category: {{ worker|default("nemo_noqueue",true) }}
  _files_out:
    input_header:               lmp_header.input
    input_minimization:         lmp_minimization.input
    coeff_file:                 coeff_hybrid.input
  _tasks:

  - _fw_name: GetFilesTask
    identifiers:
    - lmp_header.input
    - lmp_minimization.input

  - _fw_name: GetFilesTask
    identifiers:
    - {{ coeff_identifier|default("SDS/in/H2O/on/AU/coeff_hybrid.lammps") }}
    new_file_names:
    - coeff_hybrid.input

  metadata:
    state: input file retrieval
    {{ render_metadata()|indent(4) }}
{% endblock %}
